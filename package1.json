{
  "scripts": {
    "docs:dev": "sudo vuepress dev docs",
    "docs:build": "sudo vuepress build docs",
    "deploy-gh": "GH=1 sudo npm run docs:build && bash scripts/deploy-gh.sh"
  },
  "dependencies": {
    "yarn": "^1.22.17"
  }
}
